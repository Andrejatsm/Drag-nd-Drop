using UnityEngine;

public class MatchId : MonoBehaviour
{
    // Identifier used to match a draggable car to its placeholder.
    // SpawnManager sets the same id on matching pairs.
    public string id;
}